// observ-lib config — edit for your deployment.
// Passed to <lib>.new(config); see the lib's README for the accepted keys
// (commonly: selector / filteringSelector, datasource, ruleSelector, uid).
{
  // selector: 'job="my-exporter"',
}
